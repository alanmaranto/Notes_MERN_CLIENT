/* import axios from "axios";
const host = "http://localhost:3000";

const api = {
  user: {
    async getUsers() {
      const url = `${host}/users`;
      return await axios.get(url)
    }
  }
};

export default api;
 */